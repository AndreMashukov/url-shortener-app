// Placeholder — required so `tsc --noEmit` finds at least one .ts file.
// Real code lives under src/rest and src/listener and is bundled by
// Serverless Framework's esbuild plugin.

export const SERVICE_NAME = "url-shortener-analytics-bff";
